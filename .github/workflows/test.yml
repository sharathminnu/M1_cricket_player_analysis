name: Test CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: Windows

    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: make -C 3_Implementation
   
